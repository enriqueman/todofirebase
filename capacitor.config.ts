import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'todofirebase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
